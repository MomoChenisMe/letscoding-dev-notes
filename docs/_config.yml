# The Site Configuration
# https://github.com/cotes2020/jekyll-theme-chirpy

# Site Settings
title: Dev Notes
tagline: 開發筆記與技術分享
description: >-
  記錄開發過程中的學習心得、技術筆記與實用技巧。

url: "https://MomoChenisMe.github.io"
baseurl: "/letscoding-dev-notes"

# Theme
theme: jekyll-theme-chirpy

# Language
lang: zh-TW

# Timezone
timezone: Asia/Taipei

# Social
github:
  username: MomoChenisMe

social:
  name: MomoChenisMe
  links:
    - https://github.com/MomoChenisMe

# Theme mode: light, dark, or empty for user preference
theme_mode: ""

# Avatar (optional)
# avatar: /assets/img/avatar.png

# Table of Contents
toc: true

# Pagination
paginate: 10

# Kramdown settings
kramdown:
  syntax_highlighter: rouge
  syntax_highlighter_opts:
    css_class: highlight
    block:
      line_numbers: true

# Collections
collections:
  tabs:
    output: true
    sort_by: order

# Defaults
defaults:
  - scope:
      path: ""
      type: posts
    values:
      layout: post
      comments: false
      toc: true
      permalink: /posts/:title/
  - scope:
      path: _tabs
      type: tabs
    values:
      layout: page
      permalink: /:title/

# SASSify
sass:
  style: compressed

# Jekyll Archives
jekyll-archives:
  enabled: [categories, tags]
  layouts:
    category: category
    tag: tag
  permalinks:
    tag: /tags/:name/
    category: /categories/:name/

# Exclude from processing
exclude:
  - "*.gem"
  - "*.gemspec"
  - docs
  - CHANGELOG.md
  - README.md
  - LICENSE
  - Gemfile.lock
  - node_modules
  - vendor
